#include <stdio.h>

/**
 * main - prints all single digit numbers of base 10 starting from 0,
 * followed by a new line
 * output - 0123456789
 * function - use putchar twice only
 * Return: 0
 */
int main(void)
{
	int nums;

	for (nums = 48; n < 58; nums++)
	{
		putchar(nums);
	}
	putchar('\n');
}
